lista de cenas para fazer.
este ficheiro não deve ser entregue com o projecto.

Urgente:
- função para ler unsigned int (falta uma função para ler unsigned int, o NIF. semelhante à de inteiros)
- função para ler floats (falta uma função que leia floats, semelhante à de inteiros)

Necessário:
- encapsulamento (criar cópias dos void* que usamos nos módulos genéricos para que não possam ser modificadas fora do módulo)
- encapsulamento (meter funções internas do módulo como static para nao poderem ser acedidas fora do módulo)
- serialização (leitura e escrita dos dados)

Pormenores:
- mover as estruturas e funções das localidades para o "módulo" de dados (dados.c e dados.h)
- remover as funções main que foram usadas para testar o módulo e que se encontram comentadas
- documentar o programa com o doxyfile
- mudar o nome de funções e variáveis para um formato standard

Bugs:
< no bugs > (secalhar ainda não temos funcionalidades suficientes para haver bugs)
